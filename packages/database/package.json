{
  "name": "@rahal/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "format": "prisma format",
    "build": "prisma generate",
    "clean": "node -e \"require('fs').rmSync('generated',{recursive:true,force:true})\"",
    "typecheck": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "7.8.0"
  },
  "devDependencies": {
    "prisma": "7.8.0",
    "typescript": "^5.9.3"
  }
}
